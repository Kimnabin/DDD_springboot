spring:
  application:
    name: ddd-spring-boot-demo
  datasource:
      url: jdbc:mysql://localhost:3306/dddspringdb
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update # convenient in Dev environment
    properties:
      hibernate:
        format_sql: true
        user_sql_comments: true
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: efe5616f4273f9
    password: 39b498969aa7c5
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    user:
      password: root
      name: root
      roles: # multiple roles
        - USER
        - ADMIN
logging:
  level:
    org.springframework.security: debug
